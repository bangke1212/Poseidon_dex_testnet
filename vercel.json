{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd vite-dex-app && npm install && npm run build",
  "outputDirectory": "vite-dex-app/dist",
  "installCommand": "echo 'Skipping root install — handled per-package'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
